{
  "name": "ex",
  "private": true,
  "scripts": {
    "build": "npm run build:react && npm run build:react2",
    "build:react": "cd react && npm install && npm run build",
    "build:react2": "cd react2 && npm install && npm run build",
    "deploy": "npm run build && npm run gh-pages",
    "gh-pages": "npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}