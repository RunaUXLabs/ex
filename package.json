{
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run build:react && npm run build:react2",
    "build:react": "cd todo-list && npm install && npm run build && cp -r dist ../dist/react",
    "build:react2": "cd todo-list && npm install && npm run build && cp -r dist ../dist/react2",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}